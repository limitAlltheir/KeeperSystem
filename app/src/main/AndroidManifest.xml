<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.limitalltheir.keepersystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".productOrder.AddOrderActivity"></activity>
        <activity android:name=".users.UserActivity" />
        <activity android:name=".report.ReportActivity" />
        <activity android:name=".productStorage.EditActivity" />
        <activity android:name=".productStorage.DetailsProductActivity" />
        <activity android:name=".productStorage.AddToStorageActivity" />
        <activity android:name=".productStorage.StorageActivity" />
        <activity android:name=".productOrder.OrderActivity" />
        <activity android:name=".auth.AuthorizationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>